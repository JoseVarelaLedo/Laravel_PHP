<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
</head>
<body>
    {{-- <h1>Phones belonging to {{ $user->name }}:</h1>
    @foreach ($user->phones as $phone)
        <ul>
            <li>
                <h2>Prefijo: {{ $phone->prefix }}</h2>
                <h3>Número: {{ $phone->phone_number }}</h3>  
                <h4>Sim: #s/n{{ $user->phoneSim->serial_number}}, company: {{ $user->phoneSim->company }} </h4>  
            </li>
        </ul>
    @endforeach    --}}
    {{-- <h1>Roles of {{ $user->name }}:</h1>
    @foreach ($user->roles as $role)
        <ul>
            <li>
                <h2>Role: {{ $role->name }}</h2>
                <h3>Added By: {{ $role->pivot->added_by }}</h3>    
            </li>
        </ul>
    @endforeach    --}}
    

</body>
</html>